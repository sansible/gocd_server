---

- name: Test GOCD Server
  hosts: all

  pre_tasks:
    - name: Update apt
      sudo: yes
      command: apt-get update
      tags:
        - build

  roles:
    - role: ansible-city.gocd_server
